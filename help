# Manual
1.exit()  ~~ Exit the process
2.quit()  ~~ Exit the process
3.test() ~~ Test the running environment
4.help() ~~ Open the manual file
5.clear() ~~ Clear the screen
6.matrixDetect() ~~ To detect whether a matrix has existed or not ">> matrixDetect(A)" (No space)
7.matrixInput() ~~ To create a matrix  ">> matrixInput(Name,row,column)" (No space)
8.matrixAdd() ~~ To add two matrixes together ">> matrixAdd(Name1,Name2,result.Name)" (No space)
9.matrixMinus() ~~ To minus two matrixes ">> matrixMinus(Name1,Name2,result.Name)" (No space)
10.matrixMulti() ~~ To multiply two matrixes together">> matrixMulti(Name1,Name2,result.Name)" (No space)
11.matrixTran() ~~ To transpose a matrix "matrixTran(Name,result.Name)" (No space)
12.matrixScal() ~~ To scale a matrix "matrixScal(Name,scale,result.Name)" (No space)
13.matrixDet()  ~~ To get the Det(Name).(No space)
14.matrixInverse() ~~ To get the (Name)^-1 "matrixInverse(Name,result.Name)" (No space)
END